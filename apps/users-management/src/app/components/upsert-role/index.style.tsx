import { Modal } from "antd";
import styled from "styled-components";
import { devices } from "../../utils";

export const UpsertRoleModal = styled(Modal)`
  .ant-modal-body {
    margin-top: 30px;
  }
`;

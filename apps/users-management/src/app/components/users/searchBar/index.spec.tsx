import '@testing-library/jest-dom';

describe('Testing Search Bar', () => {
  // it('should render successfully', () => {
  //   const { baseElement } = render(<SearchBar />);
  //   expect(baseElement).toBeTruthy();
  // });
  // it('Checking search bar input field', () => {
  //   render(<SearchBar />);
  //   const input = screen.getByTestId(
  //     'slidelist-search-bar-input'
  //   ) as HTMLInputElement;
  //   fireEvent.change(input, { target: { value: 'xyz' } });
  //   expect(input.value).toBe('xyz');
  // });
  // it('Checking search by select option for search bar', () => {
  //   render(<SearchBar />);
  //   const selectContainer = screen.getByTestId('slidelist-select-search-type');
  //   expect(selectContainer).toBeInTheDocument();
  //   const selectedOptionElement = selectContainer.getElementsByClassName(
  //     'ant-select-selection-item'
  //   )[0];
  //   expect(selectedOptionElement).toHaveTextContent(
  //     ESearchOptionLabel.SLIDE_NUMBER
  //   );
  // });
});
